diff --git a/node_modules/react-native-enriched-markdown/android/src/main/java/com/swmansion/enriched/markdown/accessibility/AccessibleMarkdownTextView.kt b/node_modules/react-native-enriched-markdown/android/src/main/java/com/swmansion/enriched/markdown/accessibility/AccessibleMarkdownTextView.kt
index bf9ffc0..3de8999 100644
--- a/node_modules/react-native-enriched-markdown/android/src/main/java/com/swmansion/enriched/markdown/accessibility/AccessibleMarkdownTextView.kt
+++ b/node_modules/react-native-enriched-markdown/android/src/main/java/com/swmansion/enriched/markdown/accessibility/AccessibleMarkdownTextView.kt
@@ -30,4 +30,15 @@ abstract class AccessibleMarkdownTextView
       super.onFocusChanged(gainFocus, direction, previouslyFocusedRect)
       accessibilityHelper.onFocusChanged(gainFocus, direction, previouslyFocusedRect)
     }
+
+    override fun onDetachedFromWindow() {
+      // Before super, and here rather than in a ViewManager's
+      // onDropViewInstance: getViewTreeObserver() only returns the WINDOW's
+      // observer while the view is still attached. Once detached it hands back
+      // the view's own floating observer, so a removal then would target the
+      // wrong observer and leave the listener on the window forever - holding
+      // this view, and through mParent the whole hierarchy above it.
+      accessibilityHelper.cleanup()
+      super.onDetachedFromWindow()
+    }
   }
diff --git a/node_modules/react-native-enriched-markdown/android/src/main/java/com/swmansion/enriched/markdown/accessibility/MarkdownAccessibilityHelper.kt b/node_modules/react-native-enriched-markdown/android/src/main/java/com/swmansion/enriched/markdown/accessibility/MarkdownAccessibilityHelper.kt
index b709380..23883f7 100644
--- a/node_modules/react-native-enriched-markdown/android/src/main/java/com/swmansion/enriched/markdown/accessibility/MarkdownAccessibilityHelper.kt
+++ b/node_modules/react-native-enriched-markdown/android/src/main/java/com/swmansion/enriched/markdown/accessibility/MarkdownAccessibilityHelper.kt
@@ -91,6 +91,16 @@ class MarkdownAccessibilityHelper(
     observer.addOnGlobalLayoutListener(listener)
   }
 
+  /**
+   * Drops the pending global-layout listener. MUST be called while the view is
+   * still attached: once detached, `textView.viewTreeObserver` is the view's
+   * own floating observer, not the window's, so the removal would target the
+   * wrong observer and the listener would stay on the window forever.
+   */
+  fun cleanup() {
+    removePendingLayoutListener()
+  }
+
   private fun removePendingLayoutListener() {
     val listener = pendingLayoutListener ?: return
     pendingLayoutListener = null

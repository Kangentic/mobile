# Claude Code
CLAUDE.local.md

# Per-project data (runtime databases, sessions, worktrees)
.kangentic/
.claude/settings.local.json
.claude/scheduled_tasks.lock
kangentic.local.json

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.tsbuildinfo
.eslintcache
.npm

# Expo / React Native
.expo/
.expo-shared/
dist/
web-build/
expo-env.d.ts
.metro-health-check*

# Native projects are generated by `npx expo prebuild` (Continuous Native
# Generation). Never hand-edit or commit them - see
# .claude/rules/expo-cng.md.
/ios/
/android/

# EAS / secrets - never commit credentials or environment files
.env
.env.*
!.env.example
credentials.json
google-services.json
GoogleService-Info.plist
*.jks
*.keystore
*.p8
*.p12
*.mobileprovision

# Local native build outputs (EAS builds run in the cloud; local builds drop these)
*.apk
*.aab
*.ipa

# Logs
/logs/
*.log

# Test artifacts
coverage/
*.lcov
test-results/
playwright-report/
.maestro/.logs/
*.maestro-report*

# Editor / OS
.DS_Store
Thumbs.db
desktop.ini
